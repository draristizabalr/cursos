[project]
name = "news-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "draristizabalr", email = "dariarra@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openai>=3.8.0",
    "pydantic>=2.13.5",
    "python-dateutil>=2.9.0.post0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
news-api = "news_api:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
